
Dependencies
------------

Basic Xlib and Xft libraries are required.
In Debian or Ubuntu:

`apt-get install libx11-dev libxmu-dev libxft-dev`


Installing alttab
-----------------

`./configure
make
make install`

