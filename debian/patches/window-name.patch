Description: Fix an "empty window name" check.
Forwarded: no
Author: Peter Pentchev <roam@ringlet.net>
Last-Update: 2023-07-15

--- a/src/gui.c
+++ b/src/gui.c
@@ -263,7 +263,7 @@
 endBottomLine:
 
     // draw label
-    if (wi->name && fontLabel) {
+    if (wi->name[0] && fontLabel) {
         int x, y, w, h;
         if (g.option_vertical) {
             x = iconW + 5;
